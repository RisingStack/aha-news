---
import FlashAlert from "../components/FlashAlert.astro";
interface Props {
  title: string;
  loadAlpine?: boolean;
  loadPristine?: boolean;
  loadHtmx?: boolean;
}

const { title, loadAlpine, loadPristine, loadHtmx } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    {loadPristine ? <script src="/pristine.js" /> : null}
    {loadAlpine ? <script src="/alpine.js" defer /> : null}
    {loadHtmx ? <script src="/htmx.js" crossorigin="anonymous" /> : null}
    <title>{title}</title>
    <style is:global>
      [x-cloak] {
        display: none !important;
      }
    </style>
  </head>
  <body>
    <FlashAlert />
    <slot />
  </body>
</html>
<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }
  html {
    font-family: system-ui, sans-serif;
    scroll-behavior: smooth;
  }

  body {
    margin: 8px;
  }

  .text-help {
    @apply text-error;
  }

  .has-danger input {
    @apply input-error;
  }
</style>
